name: crackmamba_env
channels:
  - nvidia
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.8.15
  - mkl=2024.0
  - pytorch-cuda=11.6
  - pytorch=1.12
  - pip
  - pip:
      - causal_conv1d==1.1.1
      - packaging
